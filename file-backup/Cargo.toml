[package]
name = "file-backup"
version = "0.1.1"
edition = "2021"
rust-version = "1.86"
description = "Copy a file to `<name>.bak-<unix-seconds>` beside it before replacing it"
license = "MIT"
repository = "https://github.com/listepo/crates-packages"

[lints.rust]
unsafe_code = "forbid"

[dev-dependencies]
rstest = "0.27.0"
tempfile = "3"
